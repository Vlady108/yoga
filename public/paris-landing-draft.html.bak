<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Йога-интенсив в Париже — черновик</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: Georgia, serif; background: #fff; color: #111; max-width: 680px; margin: 0 auto; padding: 0 0 80px; }

    .placeholder-img   { background: #f0f0f0; border: 2px dashed #bbb; display: flex; align-items: center; justify-content: center; color: #999; font-family: monospace; font-size: 12px; text-align: center; padding: 12px; }
    .placeholder-video { background: #e8e8e8; border: 2px dashed #999; display: flex; align-items: center; justify-content: center; color: #666; font-family: monospace; font-size: 12px; text-align: center; padding: 12px; }

    section { padding: 48px 24px; }
    section + section { border-top: 1px solid #e8e8e8; }
    h1 { font-size: 36px; font-weight: normal; line-height: 1.2; margin-bottom: 20px; }
    h2 { font-size: 24px; font-weight: normal; margin-bottom: 16px; line-height: 1.3; }
    h3 { font-size: 17px; font-weight: bold; margin-bottom: 8px; }
    p  { font-size: 16px; line-height: 1.75; color: #333; margin-bottom: 14px; }
    p:last-child { margin-bottom: 0; }
    .eyebrow { font-family: monospace; font-size: 11px; text-transform: uppercase; letter-spacing: 0.15em; color: #888; margin-bottom: 14px; display: block; }
    .hero { padding: 0; }
    .hero-body { padding: 40px 24px 48px; }
    .hero-meta { display: flex; gap: 24px; flex-wrap: wrap; margin-top: 24px; }
    .hero-meta-item { border-left: 2px solid #111; padding-left: 12px; }
    .hero-meta-item .hm-label { font-family: monospace; font-size: 10px; text-transform: uppercase; letter-spacing: 0.1em; color: #888; }
    .hero-meta-item .hm-val   { font-size: 15px; margin-top: 2px; }
    .program-item { border: 1px solid #ddd; padding: 20px; margin-bottom: 14px; }
    .program-item .pi-num { font-family: monospace; font-size: 11px; color: #888; margin-bottom: 6px; }
    .program-item .pi-duration { font-size: 12px; color: #888; margin-top: 6px; font-family: monospace; }
    .expert { display: grid; grid-template-columns: 120px 1fr; gap: 24px; align-items: start; margin-bottom: 40px; }
    .expert:last-child { margin-bottom: 0; }
    .expert-stats { margin-top: 12px; display: flex; flex-wrap: wrap; gap: 8px; }
    .stat { background: #f5f5f5; padding: 6px 12px; font-size: 13px; font-family: monospace; }
    .reviews { display: grid; grid-template-columns: 1fr 1fr; gap: 14px; }
    .review { border: 1px solid #e0e0e0; padding: 18px; font-size: 14px; line-height: 1.65; }
    .review-author { font-family: monospace; font-size: 11px; color: #888; margin-top: 10px; }
    .form-wrap { background: #f7f7f7; padding: 32px 24px; }
    .form-group { margin-bottom: 16px; }
    label { display: block; font-size: 13px; color: #666; margin-bottom: 6px; font-family: monospace; text-transform: uppercase; letter-spacing: 0.08em; }
    input[type=text], input[type=tel] { width: 100%; border: 1px solid #111; padding: 14px; font-size: 16px; font-family: Georgia, serif; background: #fff; }
    .btn { display: block; width: 100%; padding: 18px; text-align: center; font-size: 16px; font-family: Georgia, serif; cursor: pointer; border: none; text-decoration: none; margin-bottom: 12px; }
    .btn-primary   { background: #111; color: #fff; }
    .btn-secondary { background: #fff; color: #111; border: 2px solid #111; }

    /* Editable */
    [data-field] { cursor: pointer; border-radius: 2px; transition: background 0.15s; position: relative; }
    [data-field]:hover { background: #fffbe6; outline: 1px dashed #f0c040; }
    [data-field]:hover::after {
      content: '✏️';
      position: absolute; top: -18px; right: 0;
      font-size: 12px; background: #fff;
      padding: 1px 4px; border: 1px solid #f0c040;
      z-index: 10;
    }
    [data-field].editing { outline: 2px solid #111; background: #fffbe6; cursor: text; }

    /* Save bar */
    #save-bar {
      position: fixed; bottom: 0; left: 0; right: 0;
      background: #111; color: #fff;
      display: flex; align-items: center; justify-content: space-between;
      padding: 14px 24px; font-family: monospace; font-size: 14px;
      transform: translateY(100%); transition: transform 0.2s; z-index: 100;
    }
    #save-bar.visible { transform: translateY(0); }
    #save-bar button { background: #fff; color: #111; border: none; padding: 8px 20px; font-size: 14px; cursor: pointer; font-family: monospace; }
    #save-bar button:hover { background: #f0f0f0; }
    #save-status { font-size: 12px; color: #aaa; }

    @media (max-width: 480px) {
      h1 { font-size: 28px; }
      .expert { grid-template-columns: 90px 1fr; gap: 16px; }
      .reviews { grid-template-columns: 1fr; }
    }
  </style>
</head>
<body>

<!-- ГЕРОЙ -->
<section class="hero">
  <div class="placeholder-img" style="height:360px;">[ ФОТО — атмосфера практики ]</div>
  <div class="hero-body">
    <span class="eyebrow" data-field="hero_eyebrow">Париж · Май 2026</span>
    <h1 data-field="hero_title">Три часа, которые изменят то, как вы живёте в своём теле</h1>
    <p style="font-size:18px;color:#444;line-height:1.7;" data-field="hero_desc">Живой интенсив по йоге, дыханию и работе с энергией. Два практика с 40 годами совокупного опыта — в одном зале, в один день.</p>
    <div class="hero-meta">
      <div class="hero-meta-item">
        <div class="hm-label">Дата</div>
        <div class="hm-val" data-field="date">[ число ] мая 2026</div>
      </div>
      <div class="hero-meta-item">
        <div class="hm-label">Место</div>
        <div class="hm-val" data-field="location">Париж, Франция</div>
      </div>
      <div class="hero-meta-item">
        <div class="hm-label">Формат</div>
        <div class="hm-val" data-field="format">~3 часа · живое</div>
      </div>
      <div class="hero-meta-item">
        <div class="hm-label">Стоимость</div>
        <div class="hm-val" data-field="price">[ цена ] €</div>
      </div>
    </div>
  </div>
</section>

<!-- БОЛЬ -->
<section>
  <span class="eyebrow" data-field="pain_eyebrow">Знакомо?</span>
  <h2 data-field="pain_title">Вы занимаетесь йогой — но что-то всё равно не на месте</h2>
  <blockquote style="border-left:3px solid #111;padding:16px 20px;margin:24px 0;font-size:17px;line-height:1.7;font-style:italic;color:#222;" data-field="pain_quote">"Хожу на классы уже год. Физически стало лучше. Но внутри — как будто топчусь на месте."</blockquote>
  <p data-field="pain_p1">Большинство практик работают с телом. Мало кто работает с тем, что за ним — с энергией, дыханием, тем слоем, который и определяет как вы себя чувствуете, принимаете решения, выстраиваете отношения.</p>
  <p data-field="pain_p2">Этот интенсив — про тот самый слой. Не теория. Не лекция ради лекции. Три часа практики, после которых вы уйдёте с инструментом, который можно использовать на следующий день.</p>
</section>

<!-- ПРОГРАММА -->
<section>
  <span class="eyebrow" data-field="prog_eyebrow">Программа</span>
  <h2 data-field="prog_title">Что происходит за три часа</h2>
  <div class="program-item">
    <div class="pi-num" data-field="prog1_num">ЧАСТЬ 1</div>
    <h3 data-field="prog1_title">Понять, что происходит в теле</h3>
    <p data-field="prog1_text">Лекция о природе праны и энергетических центрах. Пранаяма, бандхи, дришти, мудры — без эзотерики, на живом языке.</p>
    <div class="pi-duration" data-field="prog1_duration">~1 час · теория + демонстрация</div>
  </div>
  <div class="program-item">
    <div class="pi-num" data-field="prog2_num">ПЕРЕРЫВ</div>
    <h3 data-field="prog2_title">Пауза и живое общение</h3>
    <p data-field="prog2_text">15 минут — чай, вопросы, знакомство с участниками.</p>
    <div class="pi-duration" data-field="prog2_duration">15 минут</div>
  </div>
  <div class="program-item">
    <div class="pi-num" data-field="prog3_num">ЧАСТЬ 2</div>
    <h3 data-field="prog3_title">Почувствовать и активировать</h3>
    <p data-field="prog3_text">Телесная практика с дыхательными техниками. Активация кундалини-энергии — не как концепции, а как живого физического опыта.</p>
    <div class="pi-duration" data-field="prog3_duration">~1.5 часа · практика</div>
  </div>
</section>

<!-- ЭКСПЕРТЫ -->
<section>
  <span class="eyebrow" data-field="exp_eyebrow">Кто ведёт</span>
  <h2 data-field="exp_title">Два практика. Разные традиции. Один результат.</h2>
  <p style="margin-bottom:32px;" data-field="exp_intro">Когда встречаются два человека с таким опытом — это редкость. Обычно такой формат доступен только на многодневных ретритах за €1500+.</p>

  <div class="expert">
    <div><div class="placeholder-img" style="width:120px;height:150px;">[ фото Влада ]</div></div>
    <div>
      <h3 data-field="vlad_name">Владислав Чангелия</h3>
      <p data-field="vlad_bio">Бывший профессиональный футболист — чемпион четырёх стран, двукратный лучший вратарь Вены. 18 лет йоги, 1000+ часов международных teacher training. Высшая тантрическая инициация в традиции Shaktipat Parampara, годы изучения священных текстов у Гуру в Индии.</p>
      <div class="expert-stats">
        <span class="stat" data-field="vlad_stat1">18 лет практики</span>
        <span class="stat" data-field="vlad_stat2">1000+ ч. обучения</span>
        <span class="stat" data-field="vlad_stat3">Ретриты в 5 странах</span>
      </div>
    </div>
  </div>

  <div class="expert">
    <div><div class="placeholder-img" style="width:120px;height:150px;">[ фото Даши ]</div></div>
    <div>
      <h3 data-field="dasha_name">Дарья Смирнова</h3>
      <p data-field="dasha_bio">Основательница школы энергопрактики "Сандхи-Хилинг". Более 20 лет изучает ведические писания, обучалась в Индии у учителей древней традиции. 1000+ учеников, авторская методика. Специализация: крийя-йога, кундалини, работа с чакрами.</p>
      <div class="expert-stats">
        <span class="stat" data-field="dasha_stat1">20+ лет опыта</span>
        <span class="stat" data-field="dasha_stat2">1000+ учеников</span>
        <span class="stat" data-field="dasha_stat3">Школа 4 года</span>
      </div>
    </div>
  </div>
</section>

<!-- ВИДЕО -->
<section>
  <span class="eyebrow" data-field="video_eyebrow">Атмосфера</span>
  <h2 data-field="video_title">Как это выглядит вживую</h2>
  <div class="placeholder-video" style="height:240px;margin-bottom:14px;">[ ВИДЕО — фрагмент с ретрита ]</div>
  <div class="placeholder-video" style="height:180px;">[ ВИДЕО — Влад или Даша о формате ]</div>
</section>

<!-- ОТЗЫВЫ -->
<section>
  <span class="eyebrow" data-field="rev_eyebrow">Участники говорят</span>
  <h2 data-field="rev_title">После ретритов и семинаров</h2>
  <div class="reviews">
    <div class="review">
      <div class="placeholder-video" style="height:130px;margin-bottom:12px;">[ видео-отзыв ]</div>
      <div class="review-author" data-field="review1_author">[ Имя, город ]</div>
    </div>
    <div class="review">
      <div class="placeholder-video" style="height:130px;margin-bottom:12px;">[ видео-отзыв ]</div>
      <div class="review-author" data-field="review2_author">[ Имя, город ]</div>
    </div>
    <div class="review">
      <span data-field="review3_text">"Практика у Влада — это не набор истощающих асан. Это энергетический поток. Ты выходишь заряженным."</span>
      <div class="review-author" data-field="review3_author">Участница, Вена</div>
    </div>
    <div class="review">
      <span data-field="review4_text">"Редко встречаешь учителя, который сочетает профессионализм с человеческими качествами."</span>
      <div class="review-author" data-field="review4_author">Участник, Барселона</div>
    </div>
  </div>
</section>

<!-- ДЛЯ КОГО -->
<section>
  <span class="eyebrow" data-field="for_eyebrow">Для кого</span>
  <h2 data-field="for_title">Вам сюда, если</h2>
  <p data-field="for_p1">✓ &nbsp;Вы уже занимаетесь йогой и хотите идти глубже</p>
  <p data-field="for_p2">✓ &nbsp;Физических практик недостаточно — хочется работы с энергией</p>
  <p data-field="for_p3">✓ &nbsp;Вы в Европе и искали живой формат с настоящими практиками</p>
  <p data-field="for_p4">✓ &nbsp;Вы новичок, который хочет начать сразу с правильной базы</p>
  <p style="margin-top:20px;color:#888;font-size:14px;" data-field="for_note">Группа небольшая — каждый получает внимание обоих ведущих.</p>
</section>

<!-- РЕГИСТРАЦИЯ -->
<section id="register">
  <span class="eyebrow" data-field="reg_eyebrow">Место</span>
  <h2 data-field="reg_title">Хочу участвовать</h2>
  <p data-field="reg_desc">Оставьте имя и номер — мы напишем, ответим на вопросы и забронируем место.</p>
  <div class="form-wrap">
    <form onsubmit="return false;">
      <div class="form-group">
        <label>Ваше имя</label>
        <input type="text" placeholder="Как вас зовут?" />
      </div>
      <div class="form-group">
        <label>Телефон / WhatsApp</label>
        <input type="tel" placeholder="+33 или другой номер" />
      </div>
      <button type="submit" class="btn btn-primary" data-field="reg_btn">Забронировать место →</button>
    </form>
    <p style="text-align:center;font-family:monospace;font-size:12px;color:#888;margin-top:12px;" data-field="reg_urgency">Мест ограничено · Небольшая группа</p>
  </div>
  <p style="font-size:14px;color:#888;margin:20px 0 12px;" data-field="reg_or">Или напишите напрямую:</p>
  <a href="https://t.me/Vlady_108" class="btn btn-primary" data-field="tg_btn1">Telegram — @Vlady_108</a>
  <a href="https://t.me/dawa_wonder" class="btn btn-secondary" data-field="tg_btn2">Telegram — @dawa_wonder (Даша)</a>
</section>

<section style="text-align:center;padding:24px;border-top:1px solid #e8e8e8;">
  <p style="font-size:12px;color:#aaa;font-family:monospace;" data-field="footer_text">vladyoga.com · @Vlady_108</p>
</section>

<!-- SAVE BAR -->
<div id="save-bar">
  <span>Есть несохранённые изменения</span>
  <div style="display:flex;gap:12px;align-items:center;">
    <span id="save-status"></span>
    <button onclick="saveAll()">Сохранить</button>
  </div>
</div>

<script>
  const SLUG = 'paris';
  let fields = {};
  let dirty = false;

  async function loadFields() {
    try {
      const res = await fetch('/api/landing-draft?slug=' + SLUG);
      const data = await res.json();
      if (data.fields) {
        fields = data.fields;
        Object.entries(fields).forEach(([key, val]) => {
          const el = document.querySelector('[data-field="' + key + '"]');
          if (el) el.textContent = val;
        });
      }
    } catch(e) { console.log('Could not load draft:', e); }
  }

  document.querySelectorAll('[data-field]').forEach(el => {
    el.addEventListener('click', function(e) {
      e.preventDefault();
      if (this.classList.contains('editing')) return;
      this.classList.add('editing');
      this.contentEditable = 'true';
      this.focus();
      const range = document.createRange();
      range.selectNodeContents(this);
      const sel = window.getSelection();
      sel.removeAllRanges();
      sel.addRange(range);
    });

    el.addEventListener('blur', function() {
      this.classList.remove('editing');
      this.contentEditable = 'false';
      const key = this.dataset.field;
      const val = this.textContent.trim();
      if (fields[key] !== val) {
        fields[key] = val;
        markDirty();
      }
    });

    el.addEventListener('keydown', function(e) {
      if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); this.blur(); }
      if (e.key === 'Escape') { this.contentEditable = 'false'; this.classList.remove('editing'); }
    });
  });

  function markDirty() {
    dirty = true;
    document.getElementById('save-bar').classList.add('visible');
    document.getElementById('save-status').textContent = '';
  }

  async function saveAll() {
    const btn = document.querySelector('#save-bar button');
    btn.textContent = 'Сохраняю...';
    btn.disabled = true;
    try {
      const res = await fetch('/api/landing-draft', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ slug: SLUG, fields })
      });
      const data = await res.json();
      if (data.success) {
        dirty = false;
        document.getElementById('save-status').textContent = '✓ Сохранено';
        setTimeout(() => {
          document.getElementById('save-bar').classList.remove('visible');
          document.getElementById('save-status').textContent = '';
        }, 2000);
      }
    } catch(e) {
      document.getElementById('save-status').textContent = 'Ошибка, попробуй ещё раз';
    }
    btn.textContent = 'Сохранить';
    btn.disabled = false;
  }

  window.addEventListener('beforeunload', e => {
    if (dirty) { e.preventDefault(); e.returnValue = ''; }
  });

  loadFields();
</script>

</body>
</html>
